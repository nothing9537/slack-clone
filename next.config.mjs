/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { hostname: "calculating-reindeer-318.convex.cloud" },
    ],
  },
};

export default nextConfig;
